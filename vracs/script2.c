#include <stdio.h>
#include <unistd.h>
#include <stdbool.h>


#include <stdio.h>
#include <unistd.h>
int     main(void)
int     main(void)
int     main(void)
int     data;
switch(data)
try = ["lancelot", "ambre", 'o', 42];
if(try(ok))
{
    if(true && false)
        return(false)
    return(0);
    return(0);
}

bool    ft_numerical_test(int nbr)
{
    if(nbr >= 'a' && nbr <= 'z')
        return(false);
    else
        return(true);
}

void    ft_check_value(int nbr)
{
    if(nbr < 0)
        printf("negatif");
    if(nbr > 0)
        printf("positif");
    if(nbr == 0)
        printf("NULL");
}

int     main(void)
{
    //nbr a tester
    int     nbr;

    printf("value -->");
    scanf("%i", &nbr);
    if(!ft_numerical_test(nbr))
    {
        printf("ERROR input value");
        return(0);
    }
    ft_check_value(nbr);
    return(0);
}
