#include <stdio.h>
#include <unistd.h>

char    *ft_strlowcase(char *str)
{
    int     index;

    index = 0;
    while(str[index])
    {
        str[index] += 
        index++;
    }
}

int     main(void)
{
    return(0);
}
